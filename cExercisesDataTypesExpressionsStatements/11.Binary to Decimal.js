function binToDec(binary){

     return digit = parseInt(binary, 2);

}
console.log(binToDec('00001001'));

//
//11110000
//
// return num.split('').reverse().reduce(function(x, y, i){
//     return (y === '1') ? x + Math.pow(2, i) : x;
// }, 0);